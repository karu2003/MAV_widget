# NetworkManager — concurrent STA+AP (single radio, e.g. MT7921).
# Background scans while connected go off-channel and disrupt both the STA
# link and AP clients.  Disable them; NM will still scan to connect/reconnect.
[wifi]
bg-scan-period=-1
